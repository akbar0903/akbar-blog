spring:
  application:
    name: akbar-blog

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_blog?characterEncoding=utf-8&userSSL=false
    username: root
    password: "#123456*"

  data:
    redis:
      port: 6379

server:
  port: 9090

# 打开mybatis plus日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
